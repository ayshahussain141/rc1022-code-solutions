/* exported isVowel */
function isVowel(char) {
  if (char === 'I' || char === 'o' || char === 'a' || char === 'u' || char === 'E') {
    return true;
  } else {
    return false;
  }
}

/*
//Define a function isVowel with one parameter char//
///write an if statement of
///char is strictly equal to the string I//
//or char is strictly equal to the string o//
///or char is strictly equal to the string a//
///char is strictly equal to the string u//
///char is strictly equal to the string E//
return true or else return false//
*/
