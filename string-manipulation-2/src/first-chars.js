/* exported firstChars */
function firstChars(length, string) {
  var word = string.slice(0, length);
  if (word.length <= length) {
    return (string, word);
  }
  return word;
}

/*
// write function firstChars to two para,eters length and string///
//make the slice methid of the string object equal to two arguments o and length
and assign it to variable word//
write am if statment if length peoperty of the word object is less than or equal to
length and return string and word//
return word after the if statment//
*/
